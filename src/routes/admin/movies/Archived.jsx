import MovieTable from './MovieTable';

const Archived = () => {
    return (
        <div>
            <MovieTable type="archived" selectable actions />
        </div>
    );
};

export default Archived;
